// The global variable
let fixedValue = 4;

function incrementer() {
  // Only change code below this line
  return fixedValue + 1;
  // Only change code above this line
}

let finalResult = incrementer();
console.log("The fixed Value is still", fixedValue);
console.log("The final result is ", fixedValue, "+ 1 which outputs", finalResult);